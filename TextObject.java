import javafx.scene.canvas.Canvas;
import javafx.scene.canvas.GraphicsContext;
/*import javafx.scene.paint.Color;
import javafx.scene.text.Font;
import javafx.scene.text.Text;



public class TextObject extends Text {
    double x;
    double y;
    String name;

    public TextObject(double x, double y,String name){
        relocate(x,y);
        textSize(TextObject);

       // drawText();
    }
    void textSize(Text text){
        text.setFont(Font.font("Times New Roman",12));
    }

    /*void drawText(){
        GraphicsContext gc = getGraphicsContext2D();
        gc.setFill(Color.BLACK);
        gc.setLineWidth(1.0);
        //gc.fillText("name",0,getWidth(),getHeight()-50);
        gc.strokeText(name,getWidth(),getHeight()-10);
        // gc.fillText("name",getWidth(),getHeight()-10,0);

    }*/
//}
